import{r as e,o as t,c as d,a as l,w as r,F as o,b as c,d as i}from"./app.2e1eaa90.js";const a={},n=l("h1",{id:"上下文机制",tabindex:"-1"},[l("a",{class:"header-anchor",href:"#上下文机制","aria-hidden":"true"},"#"),c(" 上下文机制")],-1),s={class:"custom-container warning"},p=l("p",{class:"custom-container-title"},"提示",-1),g=c("-D, -E, -g 三个选项的最低使用权限为 3 级。你可以通过配置 "),u=c("authority.context"),h=c(" 修改这个行为。"),x=i('<p>Koishi 的所有问答都是被存储在一起的，但是我们往往需要让某些问题在特定的频道生效，同时让另一些问题在所有频道生效。上下文机制就是为了解决这类问题而出现的。</p><h2 id="基本用法" tabindex="-1"><a class="header-anchor" href="#基本用法" aria-hidden="true">#</a> 基本用法</h2><p>当修改现有的问答时，你可以通过以下参数完成相应的设置：</p><ul><li><code>-e</code>：在已有基础上允许当前频道</li><li><code>-E</code>：设置为全局允许</li><li><code>-d</code>：在已有基础上禁止当前频道</li><li><code>-D</code>：设置为全局禁止</li><li><code>-De</code>：设置为只允许当前频道</li><li><code>-Ed</code>：设置为只禁止当前频道</li></ul><p>如果你想控制一个一般的频道而不是当前频道，你可以使用下面的语法：</p><ul><li><code>-eg 123</code>：在已有基础上允许频道 123</li><li><code>-Eg 123</code>：设置为全局允许，除了频道 123</li><li><code>-dg 123</code>：在已有基础上禁止频道 123</li><li><code>-Dg 123</code>：设置为全局禁止，除了频道 123</li></ul><p>这里的 <code>-g</code> 也可以替换成由半角逗号隔开的多个<strong>频道标识符</strong>（例如 QQ 中的<strong>群号</strong>，开黑啦中的 <strong>#频道名</strong>），效果类似不再赘述。</p><p>最后，如果不进行任何设置则不会修改原问答的上下文。</p><p>当添加新的问答时，默认情况下的行为相当于 <code>-e</code>，即如果原问答不存在，则创建一个只允许当前频道的新问答，否则在原问答的基础上额外允许当前频道。你也可以手动覆盖这个默认行为，例如 <code># 问题 回答 -E</code> 的效果是添加一个全局允许的问答。</p><p>在搜索问答时，上面的语法依然有效，不过意义不同：</p><ul><li><code>-e</code>：匹配允许当前频道的问答</li><li><code>-E</code>：匹配全局允许的问答</li><li><code>-d</code>：匹配禁止当前频道的问答</li><li><code>-D</code>：匹配全局禁止的问答</li><li><code>-G</code>：忽略上下文机制</li></ul><p>此时默认情况下的行为仍然相当于 <code>-e</code>。如果你希望搜索所有的问答，忽略上下文造成的影响，可以使用 <code>-G</code> 参数。</p><p>此外，在私聊时，由于没有“频道”的概念，因此 <code>-e</code> 的默认行为将失效，你需要手动指定（或忽略）上下文。</p><h2 id="深入原理" tabindex="-1"><a class="header-anchor" href="#深入原理" aria-hidden="true">#</a> 深入原理</h2><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>本节将介绍上述上下文机制的内部原理。如果对此不感兴趣可以跳过。</p></div><p>在 Koishi 的教学系统中，任何问答的上下文都可以被理解为两个参数：groups 和 reversed。前者表示<strong>生效或者不生效的频道列表</strong>，而后者表示<strong>是否对匹配结果取反</strong>。举个例子，如果一个问答只允许两个频道 123, 456，则其对应的 groups 为 <code>123,456</code>，reversed 为假。如果一个问答全局允许，则其对应的 groups 为空，reversed 为真。</p><p>而当我们需要修改或者查询问答时，还涉及第三个参数 partial。这个参数表示<strong>是否部分匹配</strong>。当 partial 为真时，对搜索请求进行完全匹配，对修改请求进行覆盖操作；当 partial 为假时，对搜索请求进行部分匹配，对修改请求进行调整操作。</p><p>例如，如果原问答的 groups 为 <code>123,456</code>，reversed 为假（即只允许 123, 456 两个频道），修改请求的 groups 为 <code>456,789</code>，reversed 为真，partial 为真（即在已有的基础上禁用 456, 789 两个频道），那么得到的新回答的 groups 为 <code>123</code>，reversed 为假（即只允许 123 一个频道），这与正常的思维相一致。</p><p>现在我们已经可以理解，上面介绍的 <code>-g</code> 选项，表达的就是 groups 的含义。但是我们在大部分情况下都不需要使用这个选项，因为这个选项的默认值足以覆盖绝大部分需求：</p><table><thead><tr><th style="text-align:center;">选项</th><th style="text-align:center;">reversed</th><th style="text-align:center;">partial</th><th style="text-align:center;">groups</th></tr></thead><tbody><tr><td style="text-align:center;">-e</td><td style="text-align:center;">false</td><td style="text-align:center;">true</td><td style="text-align:center;">当前频道</td></tr><tr><td style="text-align:center;">-E</td><td style="text-align:center;">false</td><td style="text-align:center;">false</td><td style="text-align:center;">全部频道</td></tr><tr><td style="text-align:center;">-d</td><td style="text-align:center;">true</td><td style="text-align:center;">true</td><td style="text-align:center;">当前频道</td></tr><tr><td style="text-align:center;">-D</td><td style="text-align:center;">true</td><td style="text-align:center;">false</td><td style="text-align:center;">全部频道</td></tr></tbody></table>',20);a.render=function(c,i){const a=e("RouterLink");return t(),d(o,null,[n,l("div",s,[p,l("p",null,[g,l(a,{to:"/plugins/teach/config.html#authority"},{default:r((()=>[u])),_:1}),h])]),x],64)};export default a;
